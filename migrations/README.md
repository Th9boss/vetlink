Place SQL migrations here.

Rules:
- Use `.sql` files only.
- Prefix filenames with an ordered timestamp, for example `20260311_160000_add_users_index.sql`.
- Each file is executed once and tracked in the `schema_migrations` table.
- Write idempotent SQL when possible.

Example:
`20260311_160000_add_example_column.sql`
